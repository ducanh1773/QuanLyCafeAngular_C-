export type stockItem={
    id:number,
    name : string,
    quantity:number,
    status : boolean,
    deleted : boolean,
    UnitOfMeasure : string , 
}

export type StockAdd = {
    id: number,
    name: string,
    quantity: number,
    status: boolean,
    deleted: boolean,
    UniOfMesure: string,
}